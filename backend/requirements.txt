# Vercel の Python ランタイム（@vercel/python）向け。
# 依存の一次情報は pyproject.toml。こちらはそれと内容を合わせて手動同期している。
django>=5.0,<6.0
djangorestframework>=3.15
django-cors-headers>=4.4
python-dotenv>=1.0
anthropic>=0.116
openai>=1.60
gunicorn>=22.0
whitenoise[brotli]>=6.7
dj-database-url>=2.2
psycopg[binary]>=3.2
