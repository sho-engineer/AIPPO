# AIPPO クローズドベータ 0.9.5-beta.1

**2026-08-19**

仮製品として試験公開するための版。前の版で「準備中」だったところを
実際に動くようにし、公開に要る運用のしくみを入れた。

---

## この版で変わったこと

### 教材

**9本すべてが始められるようになりました。** 前の版では2本だけで、
残り7本は一覧に出るものの「準備中」でした。

### 学習を続けるための機能

| | |
| --- | --- |
| 学習リマインダー | 2日あいた人へメールを送ります。**設定から切れます**。前の版はつまみだけあって、入れても何も起きませんでした |
| 復習 | 一度終えた教材を、忘れるころにもう一度すすめます |
| 学習の記録 | 進めた教材と、AIが作った文章を見返せます |
| 修了証 | コースを全部終えると出ます。中身はAIPPOの記録で、公的な資格ではありません |
| 探す | 「メール」「議事録」のような**やりたいことの言葉**で教材を絞れます |
| あとで見る | 気になった教材に目印を付けて取っておけます |

### ログイン

- **パスキー**でログインできるようになりました（指紋・顔・画面ロック）
- ログインしたままにする期間に上限を設けました（既定90日）

### 運用（公開のために要ったもの）

- 管理画面を**決めた接続元からだけ**開けるようにしました（`ADMIN_ALLOWED_IPS`）
- 管理画面で**誰がどの学習者の記録を見たか**を残すようにしました。
  Django 標準の記録は「見ただけ」を残さないため。記録に中身は書きません
- 古いデータの自動削除と学習リマインダーを、Vercel Cron から
  1日1回動かせるようにしました
- 運営者情報を、配置するときに入れられるようにしました（`VITE_OPERATOR_*`）
- `manage.py preflight` を足しました。**公開に要る設定が揃っているかを
  1コマンドで確かめられます**

### 正直にしたところ

言語設定は、選べるのに訳文が1語も無い状態でした。外部連携・
サブスクリプション・ヘルプと同じ「準備中」で止めました。
選ばせてから「効きません」と断るのが、いちばん心証が悪いためです。

## この版で直したもの

- **取引が壊れる不具合。** 同じ教材に二重で目印を付けたとき、
  例外の拾い方が原因で、その後の問い合わせが全部落ちる状態でした
- **教材一覧が真っ白になる不具合。** 修了証の応答が想定と違う形
  （経路の設定違い、間に挟まる proxy のエラーページなど。どれも 200 で返る）
  だと、一覧ごと表示できなくなっていました
- 通知の設定を変えると、表示名が消えることがある不具合

## 分かっている制限

- **規約の運営者情報は空のままです。** `VITE_OPERATOR_NAME` /
  `_ADDRESS` / `_CONTACT` を入れるまで、画面に「（公開前に記入）」と出ます。
  身内に配るあいだは空で構いませんが、一般に告知するなら埋めてください
- 外部連携（Google / LINE）は「準備中」として押せない形にしてあります
- 課金・サブスクリプションはありません（憲章の「明示的に作らないもの」）
- 画面の言葉は日本語のみです

## 公開の手順

`docs/release-checklist.md` に上から順にまとめました。
確かめ方は1つに寄せてあります。

```bash
cd backend
python manage.py preflight
```

NG が0件になれば、設定は揃っています。

## 確かめたこと

| | |
| --- | --- |
| バックエンド | 605件 |
| フロントエンド | 347件 |
| アクセシビリティ | axe-core で18画面。違反なし（WCAG 2.1 A/AA） |

---

# AIPPO クローズドベータ 0.9.4-beta.1

**2026-08-05**

AIに興味はあるものの、何をすればよいか分からない人が、実際に触りながら
使い道を見つけるための学習アプリ。最初の外部公開版。

---

## この版でできること

**登録しなくても最後まで使えます。** 作ったものが手元に残ってから、
残したくなった人だけが登録します。

| | |
| --- | --- |
| 教材 | 9本を用意し、**2本を公開**（AI活用診断 / 文章を分かりやすくする）。残りは一覧に出しますが始められません |
| 学習の流れ | 成果物ファースト。説明を先に読ませず、1つ選ぶだけで最初の結果まで届かせます |
| アカウント | 登録・ログイン・パスワード再設定・退会。登録前の記録はそのまま引き継がれます |
| ログインの方法 | メール＋パスワード / Google / LINE。鍵を入れた先だけボタンが出ます |
| 端末をまたぐ | 別の端末でログインすると、続きから始められます |
| 教材の編集 | Django 管理画面から直せます。公開前の検査つき |
| 完了時アンケート | 3問（選択式）。答えは管理画面の「アンケートの回答」に集計されます |
| 安全 | パスワード・APIキー・カード番号は送信前に止めます |

## 分かっている制限

- **教材は2本だけ**です。残り7本は準備中と表示されます
- **規約の運営者情報が空欄**です（名称・所在地・連絡先）。
  埋めないと画面に「（公開前に記入）」と出ます
- 通知の配信、課金はありません（ログインのGoogle・LINE連携は別で、あります）
- 教材の多言語化はしていません（画面の言葉だけ切り替わります）

## この版で直したもの（デプロイ前に見つけたもの）

PostgreSQL と gunicorn で実際に動かして、次の2つが見つかりました。
どちらも **SQLite では起きず、本番の組み合わせでだけ出る**ものです。

- **AI機能が PostgreSQL で全部 500 になっていた。**
  利用回数を数える欄が 64 文字なのに、学習者ごとの名前は 72 文字でした。
  SQLite は長さを無視して書けるので手元では気づけません。
  欄を広げました（マイグレーション `lessons.0008`）
- **メール設定を忘れたまま起動できてしまい、`/health/ready` が
  503 を返し続けていた。** 起動時の関門が環境変数のほうを見ていて、
  「指定しなかった」場合に素通りしていました。振り分けが永久に付かず、
  ログにも理由が出ない止まり方です。実際に使う送り口を見るようにしました
- あわせて、`DATABASE_URL=sqlite:///...` と書いたときに
  「database is locked」対策（WAL・待ち時間）が黙って外れる問題も直しました

## 公開する前に必ずやること

1. 運営者情報を埋める（`frontend/src/content/legal.ts` の `OPERATOR`）
2. 環境変数を入れる（README「公開するときに必ず確認すること」）
   - 入れないと**起動しません**: `DJANGO_SECRET_KEY` `DJANGO_ALLOWED_HOSTS`
     `FRONTEND_URL`、smtp を使うなら `EMAIL_HOST` `DEFAULT_FROM_EMAIL`
   - 入れないと**事故になります**: `AI_RUNS_PER_DAY`（最後の安全弁）、
     `SECURE_SSL_REDIRECT=true`、LB配下なら `TRUST_FORWARDED_FOR=true`
3. Google / LINE を使うなら、各社の管理画面へ**戻り先を登録**する
   （`<BACKEND_URL>/api/v1/accounts/social/google/callback/` と
   `.../line/callback/`）。登録した文字列と1文字でも違うと、
   向こうの画面で断られます。鍵を入れない先はボタンごと出ません
4. 管理者を作り、`/admin/` の露出をどうするか決める
5. DBのバックアップを用意する（退会もデータ削除も取り消せません）
6. `manage.py prune_data` を1日1回動かす設定をする
   （プライバシーポリシーに「180日で削除」と書いてあります）

**AIの鍵を入れる前に `AI_RUNS_PER_DAY` を決めてください。**
入れた瞬間から費用が出ます。まず `AI_PROVIDER=mock` で通しの動作を
確かめ、そのあと本物へ切り替えるのが安全です。

## 出したあと最初に見るもの

1. `/health/ready` が `ok`（database / ai / email の3つとも true）
2. 教材一覧に9本出て、公開した2本だけ始められる
3. 登録 → 確認メールが届く → 別の端末でログイン → 続きが出る
4. 管理画面で教材の文言を直す → 画面を再読み込みして反映される
5. レッスンを1本終える → アンケートに答える →
   `/admin/lessons/verificationsummary/` の「アンケートの回答」に出る

**アンケートの質問文は、ベータの最中に書き換えないでください。**
集計は質問文をそのまま見出しにして数えます。書き換えると同じ質問が
別の行に分かれ、前後で比べられなくなります。

## 確かめたこと

| | |
| --- | --- |
| バックエンド | 376件（**PostgreSQL / SQLite の両方**で通しています） |
| フロントエンド | 231件 |
| E2E | 64件（desktop / mobile。うち探索テストは本物の Django に当てる） |
| アクセシビリティ | 19画面で WCAG 2.1 A/AA 違反なし |
| lint / 型 | ruff・ESLint・tsc すべてクリーン |
| 本番設定 | `manage.py check --deploy` が無指摘 |
| 本番の組み合わせ | **gunicorn（3 worker）＋ PostgreSQL 16** で通し動作を確認 |

教材9本すべてを実ブラウザ・実サーバーで完走し、再読み込みでの再開、
AIが落ちたとき、上限に達したとき、近日公開の教材への直接要求まで
確かめています。

**確かめていないこと**: 実際のAIプロバイダ（鍵がないため mock のみ）、
実際のSMTP送信、**Google と LINE の本物の往復**（本物の鍵がないため、
こちらから出す要求と戻ってきたときの扱いまで。向こうの画面で承認する
部分は未確認）。Preview 環境で最初に確かめてください。

**匿名の書き込みには CSRF の検査がかかりません**（学習イベントと
アンケート）。DRF の作りで、合言葉の検査は*ログイン中の人*の書き込みに
対して働きます。ログインが要る操作（表示名の変更・退会など）は
守られていることを確認済みです。匿名側で起こりうるのは、
よそのページから開かせてアンケートの答えを1つ書き込ませること、
つまり集計の汚れまでで、読み取りや乗っ取りはできません。
ベータの規模では許容しますが、把握したうえで出してください。
