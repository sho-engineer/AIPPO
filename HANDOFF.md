# AIPPO 引き継ぎメモ

このZIPは `sho-engineer/AIPPO` へそのまま push できる状態のリポジトリです
（このセッションからは push が 403 で弾かれるため、ZIP で渡しています）。

```bash
unzip aippo.zip -d aippo && cd aippo
git init && git add -A
git commit -m "AIPPO MVP"
git remote add origin https://github.com/sho-engineer/AIPPO.git
git push -u origin main
```

---

## 1. 今回の変更：トップをゲームのタイトル画面にした

配色もロゴも直したのに単調に見えていました。残っていた理由は
**画面が一枚の平らな板だったこと**で、直し方は2つでした。

### 1-1. 奥行きを足す

タイトル画面の背景を4層に分けました（`src/components/TitleBackdrop.tsx`）。

| 層 | 中身 | 速さ |
| --- | --- | --- |
| 空 | グラデーション | 動かない |
| 遠景 | 強くぼかした大きな面 3つ | 19秒でゆっくり |
| 丘 | ポーが立つ地面。2枚重ね | 動かない |
| 手前 | 泡 8つ・きらめき 4つ | 13〜25秒でばらばら |

決め事が3つあります。

- **速さを層ごとに変える。** 全部同じ速さで動かすと、
  一枚の板が揺れているようにしか見えません。
- **奥は薄く、手前は濃く。** 逆にすると前後が入れ替わって見えます。
- **丘の左右のはみ出し方をずらす。** 左右対称に置くと、
  そこだけで作り置きの見た目に戻ります。

泡は位置・大きさ・速さ・遅れをわざと不揃いにしてあります。
等間隔に並べると模様になってしまい、「機械が置いた」感じが
かえって強く出るためです。
遅れには**負の値**を入れて途中から始めています。開いた瞬間に
全部が下から一斉に上がると、演出が始まったことがばれます。

### 1-2. 動かす

動かす目的は賑やかしではなく、次の3つに限っています。

| 目的 | 何をしているか |
| --- | --- |
| 生きている感じを出す | ポーが呼吸するように浮く。**浮いた分だけ足元の影が縮む** |
| 押せる場所を教える | 「はじめる」から輪が広がる |
| 順番を教える | ロゴ → 読み → キャッチコピー → ポー → ボタン の順に現れる |

動きはすべて CSS で、キーフレームは `tailwind.config.js` に置いてあります。
ライブラリを足すほどの見返りが無く、まとめて止められる形が扱いやすいためです。

> 実装で引っかかった点が2つあります。
> ・**登場と待機は別の要素に分ける**（同じ要素に両方書くと後勝ちで片方しか効かない）
> ・**登場には `animation-fill-mode: both` を付ける**
> （付けないと、遅らせて出す要素が最初の一瞬だけ元の位置に見える）

### 1-3. 動きが苦手な人には出さない

動きで気分が悪くなる人がいます。端末が「視差効果を減らす」設定のとき
（`prefers-reduced-motion: reduce`）は、`src/index.css` で全部の動きを
一瞬で終わらせています。

`animation: none` にはしていません。それだと遅らせて出している要素が
**現れないまま消えます**。
`e2e/a11y.spec.ts` が、この設定で
（a）動いているものが1つも無いこと
（b）それでも「はじめる」が見えていること
の両方を検査します。

### 1-4. 手順は「すごろくの盤面」へ

番号つきカードを3枚横に並べるのをやめ、**右へ行くほど高い位置**に置いて
点線でつなぎました。名前の由来が「AI＋一歩」なので、登っていく形そのものを
見せています。上がり（3番目）だけ色を変えて、段差が読み取れるようにしました。

狭い画面では縦に積み、左端の点線でつなぎます。

### 1-5. 1画面に収める

タイトル画面と名乗る以上、スクロールしないと始められないのでは形になりません。
`e2e/a11y.spec.ts` が、ロゴ・ポー・「はじめる」の3つが
最初の1画面に収まっていることを検査します。

中身は縦の**真ん中**に置いています。下端へ寄せると、
画面が縦に長い端末でボタンが枠の外へ出ました。

---

## 2. 今回いっしょに直した不具合

| 症状 | 原因と対処 |
| --- | --- |
| **診断を終えた人にだけ 500**（`database is locked`） | 保存が `update_or_create`、つまり1つのトランザクションの中で「読んでから書く」形でした。読んだ時点では共有ロックしか持っておらず、書き込みへ昇格しようとして別の接続とかち合うと、待ち時間の設定に関係なくその場で失敗します。UPDATE と INSERT を1文ずつに分けました |
| 送り先の案内が注記と重なって文字が二重になる | 画面の下端へ貼り付けていたため、縦 800px 前後の画面で中身の下端とぶつかっていました。並びの中へ戻しました |
| 画面の書き出しがボタンの無い絵を保存する | 登場の動きが終わるのを待っていませんでした |
| `pip install -e ".[dev]"` が通らない | setuptools が `apps` / `config` / `tests` を別々のパッケージとみなして止まっていました。これはライブラリではなくアプリなので、配る中身が無いことを宣言しました |

**回帰テストは、直す前に落ちることを確かめてから入れています。**
たとえば同時保存のテストは、`update_or_create` に戻すと
`error:OperationalError` が2件出て落ちます。

---

## 3. 見た目の決めごと

色・文字サイズ・動きは **`frontend/tailwind.config.js` の1か所だけ**に
定義しています。コンポーネントに生の色コード（`#0079AC` など）は
書かないでください。

色はすべてポーの絵から取っています。

| 名前 | 値 | 由来・用途 |
| --- | --- | --- |
| `brand` | `#0079AC` | ポーの青。白文字を載せるので濃さを上げてある |
| `brand.bright` | `#18E4FC` | アンテナの水色。**飾り専用**、文字には使わない |
| `joy` | `#B8425A` | ほおの色を文字に使える濃さにしたもの。すごろくの上がりにも使用 |
| `ink` | `#0A1E3A` | 輪郭と目の紺 |
| `canvas` | `#F2FAFE` | 白い体になじむ、ほんのり青い白 |

`brand.bright` を文字色に使うと白との比が 2.57 で基準（4.5）を割ります。
**`e2e/a11y.spec.ts` が実際にこれを検知して落ちます。**
配色を触るときは、この検査を通ることが条件です。

書体は **Zen Maru Gothic**（丸ゴシック、OFL / 再配布可）。
日本語で「親しみやすさ」を出す一番効く手段です。
ただし日本語フォントは重いので、`scripts/build-fonts.py` で
`unicode-range` を使って16分割し、**画面に出た文字の分だけ**取りに行きます。

---

## 4. 画像の作り直し

```bash
cd frontend
npm run poe /path/to/元画像のディレクトリ   # ポーの表情6枚＋トップの主役
npm run fonts                                # 丸ゴシックの分割
python3 scripts/build-logo.py ロゴシート.png  # ロゴ3種
```

`build-poe.py` の要点は3つです。

1. **表情6枚は同じ枠に収める。** 体の大きさと足元の位置をそろえないと、
   表情が切り替わるたびにポーが跳ねて見えます。
2. **飾りの絵は余白を切り詰める。** 表情と同じ正方形に収めると3割が
   透明な余白になり、大きく見せたい場所で絵が小さくしか出せません。
3. **元絵に薄く敷かれた光を落とす。** 元絵は四隅まで透明ではなく、
   ごく薄い白がかかっています。そのままだと濃い色の面に載せたときに
   四角い靄として見え、余白の切り詰めもできません。

`build-logo.py` は透過の抜き方が肝心です。キャラクターの体が白いので、
「白いピクセルを消す」やり方だと体まで消えます。
**画像の外周から内側へ塗りつぶす**方式にしてあります。

---

## 5. 動かし方

### 前提
- Python 3.12+ / Node 20+

### バックエンド

```bash
cd backend
python -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]"
python manage.py migrate
python manage.py runserver
```

`.env` の `CORS_ALLOWED_ORIGINS` に**実際に使うポートを全部**書いてください。
ここが漏れていると、ブラウザ側は何のエラーも出さずに POST が握り潰されます。

### フロントエンド

```bash
cd frontend
npm install
npm run dev
```

---

## 6. テスト

| 種類 | 件数 | コマンド |
| --- | --- | --- |
| バックエンド（pytest） | 126 | `cd backend && pytest` |
| フロント（Vitest） | 95 | `cd frontend && npm test` |
| E2E（Playwright） | 33 × 2端末 = 66 | `cd frontend && npm run test:e2e` |

**全部緑です。** E2E は本番と同じビルド成果物（`E2E_TARGET=build`）に対して、
本物の Django を立てた状態で通しています。

```bash
# 探索テストは本物のバックエンドに当てるので、Django を先に起動する
cd backend && AI_RUNS_PER_IP_PER_DAY=0 AI_RUNS_PER_DAY=0 python manage.py runserver 127.0.0.1:8000
cd frontend && E2E_TARGET=build npm run test:e2e
```

AI 回数の上限に自分で引っかかるので、テスト時は上限を切ってください
（CI では設定済みです）。

`e2e/a11y.spec.ts` は `@axe-core/playwright` で WCAG 2.1 A/AA を自動検査します。
**これは体裁のためのテストではなく、実際に何度も本物の不具合を捕まえています。**
配色やマークアップを変えたら必ず通してください。

画面を書き出したいときは：

```bash
CAPTURE_SCREENSHOTS=1 npx playwright test e2e/screenshots.spec.ts
```

---

## 7. 作りの要点（引き継ぐ人向け）

### 会員登録なしで続きから再開できる仕組み
`learner_key` という UUID を HttpOnly Cookie（90日）で配っています。
User モデルはありません。登録の手間で初心者を落とさないためです。

### AI 費用の歯止め（3段）
全体の1日あたり／IP ごとの1日あたり／1レッスンあたり。
IP は生のままでは保存せず、`HMAC(SECRET_KEY, ip)` にしています。

カウントも診断の保存も、**条件付きの単一文**でやっています。
SQLite で「読んでから書く」形にすると、書き込みが重なった瞬間に
`database is locked` で 500 になるためです（今回それを1件直しました）。

### ストリーミング（SSE）
`views_stream.py` に `EventStreamRenderer` を置いています。
これが無いと DRF が `Accept: text/event-stream` を 406 で弾きます。
curl は `*/*` を送るので気付けません。**ブラウザだけで壊れる**種類の不具合でした。

### ポーは押せない
学習中のポーは画面に固定して重ねるので、下にあるボタンのタップを
奪わないよう `pointer-events-none` を付けています。
吹き出しの高さにも上限（`max-h-24`）があります。

---

## 8. できていないこと・注意点

- **Docker イメージのビルドは未検証**です。
  この環境からは Docker Hub へ出られず、`docker build` が通せませんでした。
  `Dockerfile` / `docker-compose.yml` の各手順は、
  同じコマンドを直接実行して動くことだけ確認しています。
- `npm run lint` は **ESLint の設定ファイルが無く動きません**（以前からです）。
  CI では実行していないので今は止まりませんが、使うなら
  `eslint.config.js` を用意する必要があります。
- `favicon.png` と `ogp.png` は手作業で作ったもので、作り直す
  スクリプトがありません。キャラクターやロゴを差し替えるときは
  この2枚も直してください。
- `logo-stacked.webp` はトップのタイトルに使っています。
  `logo-horizontal.webp` は診断・レッスンのヘッダー用です。
- 本番投入時は `DEBUG=False` にしてください。
  そのとき `SECRET_KEY` が開発用のまま・短すぎる・`ALLOWED_HOSTS` 未設定
  のいずれかなら、起動時に `ImproperlyConfigured` で止まります（意図的な作りです）。
- DB は `DATABASE_URL` を設定すれば PostgreSQL に切り替わります。

---

## 9. 画面の見本

`frontend/screenshots/` に、デスクトップとモバイルの通し画面が入っています
（トップ → 診断 → おすすめ → レッスン → 実務で試す → ふりかえり → 完了）。
本番と同じビルド成果物に対して E2E が通ったときの画面そのものです。
